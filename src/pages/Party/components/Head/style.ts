import styled from "styled-components";

export const Head = styled.div`
  display: flex;
  align-items: center;
  gap: 1rem;
  font-size: 1.6rem;
  color: var(--white);
  h2{
    font-weight: 500;
    color: var(--white);
  }
  svg{
    font-size: 3rem;
  }
`;